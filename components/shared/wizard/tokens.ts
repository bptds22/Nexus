/* ═══════════════════════════════════════════════════════════════
   Wizard tokens — verbatim from AthleteWizardMobile.tsx :153-154.
   Shared by the row primitives in ./rows.tsx and by call sites
   in the coach wizard (DualPickerRow + any future row that wants
   to match the same label/value typography without importing the
   full rows kit).
═══════════════════════════════════════════════════════════════ */

export const labelCls = "text-[14px] text-white/55";
export const valueCls = "text-[15px] text-white font-semibold";
